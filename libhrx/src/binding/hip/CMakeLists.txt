################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libhrx/src/binding/hip/BUILD.bazel                                           #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

iree_cc_library(
  NAME
    vmm_test_api_headers
  HDRS
    "api.h"
  DEPS
    libhrx_defs
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    printf_format
  HDRS
    "printf_format.h"
  SRCS
    "printf_format.c"
  DEPS
    iree::base
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    hostcall_packet
  HDRS
    "hostcall_packet.h"
  DEPS
    iree::base
    iree::base::internal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    hostcall_message
  HDRS
    "hostcall_message.h"
  SRCS
    "hostcall_message.c"
  DEPS
    ::hostcall_packet
    iree::base
    iree::base::internal::arena
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    blocking_printf_protocol
  HDRS
    "blocking_printf_protocol.h"
  SRCS
    "blocking_printf_protocol.c"
  DEPS
    ::hostcall_message
    ::printf_format
    iree::base
    iree::hal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    blocking_printf_provider
  HDRS
    "blocking_printf_provider.h"
  SRCS
    "blocking_printf_provider.c"
  DEPS
    ::blocking_printf_protocol
    iree::base
    iree::hal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

iree_cc_library(
  NAME
    execution_resource
  HDRS
    "api.h"
    "execution_resource.h"
  SRCS
    "execution_resource.c"
  DEPS
    iree::base
    iree::hal
    libhrx::src::binding::common
    libhrx::src::binding::common::execution_resource
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    execution_resource_vmm_test_instrumented
  HDRS
    "api.h"
    "execution_resource.h"
  SRCS
    "execution_resource.c"
  DEPS
    iree::base
    iree::hal
    libhrx::src::binding::common::common_vmm_test_instrumented
    libhrx::src::binding::common::execution_resource
    libhrx_defs
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    launch_params
  HDRS
    "api.h"
    "launch_params.h"
  SRCS
    "launch_params.c"
  DEPS
    iree::base
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    launch_params_vmm_test_instrumented
  HDRS
    "api.h"
    "launch_params.h"
  SRCS
    "launch_params.c"
  DEPS
    iree::base
    iree::hal
    libhrx::src::binding::common::common_vmm_test_instrumented
    libhrx_defs
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    handle_registry
  HDRS
    "handle_registry.h"
  SRCS
    "handle_registry.c"
  DEPS
    iree::base
    iree::base::threading
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

iree_cc_library(
  NAME
    execution_resource_descriptor
  HDRS
    "execution_resource_descriptor.h"
  SRCS
    "execution_resource_descriptor.c"
  DEPS
    ::execution_resource
    ::handle_registry
    iree::base
    iree::base::threading
    iree::hal
    libhrx::src::binding::common
    libhrx::src::binding::common::execution_resource
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    execution_resource_descriptor_vmm_test_instrumented
  HDRS
    "execution_resource_descriptor.h"
  SRCS
    "execution_resource_descriptor.c"
  DEPS
    ::execution_resource_vmm_test_instrumented
    ::handle_registry
    iree::base
    iree::base::threading
    iree::hal
    libhrx::src::binding::common::common_vmm_test_instrumented
    libhrx::src::binding::common::execution_resource
    libhrx_defs
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    execution_context
  HDRS
    "execution_context.h"
    "vmm.h"
  SRCS
    "execution_context.c"
  DEPS
    ::execution_resource
    ::execution_resource_descriptor
    ::stream
    iree::base
    iree::base::threading
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    execution_context_vmm_test_instrumented
  COPTS
    "-DIREE_HIP_VMM_TESTING"
  HDRS
    "execution_context.h"
    "vmm.h"
  SRCS
    "execution_context.c"
  DEPS
    ::execution_resource_descriptor_vmm_test_instrumented
    ::execution_resource_vmm_test_instrumented
    ::stream_vmm_test_instrumented
    iree::base
    iree::base::threading
    iree::hal
    libhrx::src::binding::common::common_vmm_test_instrumented
    libhrx_defs
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    stream
  HDRS
    "api.h"
    "stream.h"
  SRCS
    "stream.c"
  DEPS
    ::handle_registry
    iree::base
    iree::base::internal
    iree::base::threading
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    stream_vmm_test_instrumented
  HDRS
    "api.h"
    "stream.h"
  SRCS
    "stream.c"
  DEPS
    ::handle_registry
    iree::base
    iree::base::internal
    iree::base::threading
    iree::hal
    libhrx::src::binding::common::common_vmm_test_instrumented
    libhrx_defs
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    amdhip64
  COPTS
    "-DIREE_HAL_STREAMING_HIP_EXPORTS"
    "-Wno-unused-variable"
  HDRS
    "api.h"
    "binding_internal.h"
    "vmm.h"
  SRCS
    "abi_stubs.c"
    "api.c"
    "api_metadata.c"
    "vmm.c"
  DEPS
    ::blocking_printf_provider
    ::execution_context
    ::execution_resource
    ::execution_resource_descriptor
    ::handle_registry
    ::launch_params
    ::stream
    iree::base
    iree::base::threading
    iree::hal
    iree::hal::drivers::amdgpu
    iree::hal::drivers::amdgpu::util::libhsa
    iree::hal::drivers::amdgpu::util::libhsa_headers
    libhrx::src::binding::common
    libhrx_defs
  SHARED
  LINKOPTS
    "-Wl,--undefined-version"
    "-Wl,--version-script=${CMAKE_CURRENT_SOURCE_DIR}/amdhip64.map"
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    amdhip64_vmm_test_instrumented
  COPTS
    "-DIREE_HAL_STREAMING_HIP_EXPORTS"
    "-DIREE_HIP_VMM_TESTING"
    "-Wno-unused-variable"
  HDRS
    "api.h"
    "binding_internal.h"
    "vmm.h"
  SRCS
    "abi_stubs.c"
    "api.c"
    "api_metadata.c"
    "vmm.c"
  DEPS
    ::blocking_printf_provider
    ::execution_context_vmm_test_instrumented
    ::execution_resource_descriptor_vmm_test_instrumented
    ::execution_resource_vmm_test_instrumented
    ::handle_registry
    ::launch_params_vmm_test_instrumented
    ::stream_vmm_test_instrumented
    iree::base
    iree::base::threading
    iree::hal
    iree::hal::drivers::amdgpu::amdgpu_test_instrumented
    iree::hal::drivers::amdgpu::util::libhsa_headers
    iree::hal::drivers::amdgpu::util::libhsa_test_instrumented
    libhrx::src::binding::common::common_vmm_test_instrumented
    libhrx_defs
  TESTONLY
  SHARED
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)
endif()

iree_cc_test(
  NAME
    execution_resource_test
  SRCS
    "execution_resource_test.cc"
  DEPS
    ::execution_resource
    iree::base
    iree::hal
    iree::hal::testing::mock_device
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx::src::binding::common
    libhrx_defs
)

iree_cc_test(
  NAME
    execution_resource_descriptor_test
  SRCS
    "execution_resource_descriptor_test.cc"
  DEPS
    ::execution_resource
    ::execution_resource_descriptor
    iree::base
    iree::hal
    iree::hal::testing::mock_device
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx::src::binding::common
    libhrx_defs
)

iree_cc_test(
  NAME
    handle_registry_test
  SRCS
    "handle_registry_test.cc"
  DEPS
    ::handle_registry
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

if(TARGET libhrx_src_binding_hip_amdhip64)
set_target_properties(libhrx_src_binding_hip_amdhip64 PROPERTIES
  OUTPUT_NAME amdhip64
  VERSION 7.9999.0
  SOVERSION 7
  POSITION_INDEPENDENT_CODE ON
  INSTALL_RPATH "$ORIGIN"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_api_name_test
  SRCS
    "hip_api_name_test.cc"
  DATA
    ::amdhip64
  DEPS
    ${CMAKE_DL_LIBS}
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64=$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_execution_resource_api_test
  SRCS
    "hip_execution_resource_api_test.cc"
  DATA
    ::amdhip64
  DEPS
    ${CMAKE_DL_LIBS}
    ::execution_resource
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64=$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_get_proc_address_test
  SRCS
    "hip_get_proc_address_test.cc"
  DATA
    ::amdhip64
  DEPS
    ${CMAKE_DL_LIBS}
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64=$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_launch_validation_api_test
  SRCS
    "${LIBHRX_SOURCE_DIR}/cts/core/amdgpu_executable_test_data.hpp"
    "${LIBHRX_SOURCE_DIR}/cts/core/amdgpu_test_data.hpp"
    "hip_launch_validation_api_test.cc"
  COPTS
    "-Wno-attributes"
  DATA
    ::amdhip64
  DEPS
    ${CMAKE_DL_LIBS}
    ::vmm_test_api_headers
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx::cts::amdgpu_executable_test_kernels
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64=$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  SANITIZER_SUPPRESSIONS
    lsan
    rocm
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_memory_pool_api_test
  SRCS
    "hip_memory_pool_api_test.cc"
  DATA
    ::amdhip64
  DEPS
    ${CMAKE_DL_LIBS}
    ::launch_params
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64=$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_memory_pool_admission_test
  SRCS
    "hip_memory_pool_api_test.cc"
  DEFINES
    "IREE_HIP_POOL_ADMISSION_TEST=1"
  DATA
    ::amdhip64_vmm_test_instrumented
  DEPS
    ${CMAKE_DL_LIBS}
    ::launch_params
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64_PHASE=$<TARGET_FILE:libhrx::src::binding::hip::amdhip64_vmm_test_instrumented>"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_virtual_memory_api_test
  SRCS
    "${LIBHRX_SOURCE_DIR}/cts/core/amdgpu_executable_test_data.hpp"
    "${LIBHRX_SOURCE_DIR}/cts/core/amdgpu_test_data.hpp"
    "hip_virtual_memory_api_test.cc"
  COPTS
    "-Wno-attributes"
  DATA
    ::amdhip64
  DEPS
    ${CMAKE_DL_LIBS}
    ::launch_params
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx::cts::amdgpu_executable_test_kernels
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64=$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_vmm_lifecycle_phase_test
  SRCS
    "hip_vmm_lifecycle_phase_test.cc"
  COPTS
    "-Wno-attributes"
  DATA
    ::amdhip64_vmm_test_instrumented
  DEPS
    ${CMAKE_DL_LIBS}
    ::vmm_test_api_headers
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64_PHASE=$<TARGET_FILE:libhrx::src::binding::hip::amdhip64_vmm_test_instrumented>"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  TIMEOUT
    900
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    hostcall_message_test
  SRCS
    "hostcall_message_test.cc"
  DEPS
    ::hostcall_message
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    blocking_printf_protocol_test
  SRCS
    "blocking_printf_protocol_test.cc"
  DEPS
    ::blocking_printf_protocol
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    blocking_printf_provider_test
  SRCS
    "blocking_printf_provider_test.cc"
  DEPS
    ::blocking_printf_protocol
    ::blocking_printf_provider
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

iree_cc_test(
  NAME
    launch_params_test
  SRCS
    "launch_params_test.cc"
  DEPS
    ::launch_params
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    printf_format_test
  SRCS
    "printf_format_test.cc"
  DEPS
    ::printf_format
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

# Point HIP DSO tests at the shared object built in this tree so they dlopen()
# the exact artifact under test, and make sure that artifact is built first.
# The build-tree path is only valid for the in-tree CTest run.
foreach(_hrx_hip_dso_test
    libhrx_src_binding_hip_hip_api_name_test
    libhrx_src_binding_hip_hip_get_proc_address_test
    libhrx_src_binding_hip_hip_launch_validation_api_test)
  if(TARGET ${_hrx_hip_dso_test})
    target_compile_definitions(${_hrx_hip_dso_test}
      PRIVATE
        "HRX_TEST_LIBAMDHIP64_PATH=\"$<TARGET_FILE:libhrx_src_binding_hip_amdhip64>\""
    )
    add_dependencies(${_hrx_hip_dso_test}
      libhrx_src_binding_hip_amdhip64
    )
  endif()
endforeach()

# The symbol version script and --undefined-version are applied through the
# generated LINKOPTS above (from linkopts in BUILD.bazel). Keep them out of
# LINK_FLAGS here so the version script is not applied twice.
if(TARGET libhrx_src_binding_hip_amdhip64)
  install(TARGETS libhrx_src_binding_hip_amdhip64
    LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
      COMPONENT "${HRX_PUBLIC_DIST_COMPONENT}"
      NAMELINK_COMPONENT "${HRX_PUBLIC_DIST_COMPONENT}"
    ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}"
      COMPONENT "${HRX_PUBLIC_DIST_COMPONENT}"
    RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
      COMPONENT "${HRX_PUBLIC_DIST_COMPONENT}")
endif()
