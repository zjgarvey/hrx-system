################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libhrx/src/libhrx/BUILD.bazel                                                #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

set(_hrx_static_platform_copts "")
if(IREE_HAL_DRIVER_AMDGPU)
  list(APPEND _hrx_static_platform_copts "-DHRX_HAS_IREE_AMDGPU_DRIVER")
endif()
set(_hrx_static_platform_deps "")
if(IREE_HAL_DRIVER_AMDGPU)
  list(APPEND _hrx_static_platform_deps iree::hal::drivers::amdgpu)
  list(APPEND _hrx_static_platform_deps iree::hal::drivers::amdgpu::registration)
  list(APPEND _hrx_static_platform_deps iree::hal::drivers::amdgpu::target::selection)
endif()
iree_cc_library(
  NAME
    hrx_static
  COPTS
    ${_hrx_static_platform_copts}
  HDRS
    "${LIBHRX_SOURCE_DIR}/src/iree_hal_compat.h"
    "buffer_table.h"
    "hrx_internal.h"
    "mem_pool.h"
    "transfer.h"
  SRCS
    "allocator.c"
    "buffer.c"
    "buffer_table.c"
    "buffer_view.c"
    "device.c"
    "event.c"
    "executable.c"
    "fence.c"
    "graph.c"
    "graph_analysis.c"
    "graph_exec.c"
    "host_allocator.c"
    "mem_pool.c"
    "pool.c"
    "queue_ops.c"
    "runtime.c"
    "semaphore.c"
    "status.c"
    "stream.c"
    "transfer.c"
    "vmm_slab_provider.c"
    "vmm_slab_provider.h"
  DEPS
    ${CMAKE_DL_LIBS}
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::core_headers
    iree::base::internal
    iree::base::internal::arena
    iree::base::threading
    iree::hal
    iree::hal::drivers::task::driver
    iree::hal::drivers::task::executable::loaders::registration
    iree::hal::memory::asan
    iree::hal::memory::passthrough_pool
    iree::hal::memory::slab_provider
    iree::hal::memory::tlsf_pool
    iree::hal::utils::profile_file
    iree::hal::utils::resource_set
    iree::io::file_handle
    iree::task::api
    libhrx_defs
    ${_hrx_static_platform_deps}
  DEFINES
    "HRX_STATIC"
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/.>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/.>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../include>"
  PUBLIC
)

set(_hrx_static_vmm_test_instrumented_platform_copts "")
if(IREE_HAL_DRIVER_AMDGPU)
  list(APPEND _hrx_static_vmm_test_instrumented_platform_copts "-DHRX_HAS_IREE_AMDGPU_DRIVER")
endif()
set(_hrx_static_vmm_test_instrumented_platform_deps "")
if(IREE_HAL_DRIVER_AMDGPU)
  list(APPEND _hrx_static_vmm_test_instrumented_platform_deps iree::hal::drivers::amdgpu::amdgpu_test_instrumented)
  list(APPEND _hrx_static_vmm_test_instrumented_platform_deps iree::hal::drivers::amdgpu::registration::registration_vmm_test_instrumented)
  list(APPEND _hrx_static_vmm_test_instrumented_platform_deps iree::hal::drivers::amdgpu::target::selection)
endif()
iree_cc_library(
  NAME
    hrx_static_vmm_test_instrumented
  COPTS
    ${_hrx_static_vmm_test_instrumented_platform_copts}
  HDRS
    "${LIBHRX_SOURCE_DIR}/src/iree_hal_compat.h"
    "buffer_table.h"
    "hrx_internal.h"
    "mem_pool.h"
    "transfer.h"
  SRCS
    "allocator.c"
    "buffer.c"
    "buffer_table.c"
    "buffer_view.c"
    "device.c"
    "event.c"
    "executable.c"
    "fence.c"
    "graph.c"
    "graph_analysis.c"
    "graph_exec.c"
    "host_allocator.c"
    "mem_pool.c"
    "pool.c"
    "queue_ops.c"
    "runtime.c"
    "semaphore.c"
    "status.c"
    "stream.c"
    "transfer.c"
    "vmm_slab_provider.c"
    "vmm_slab_provider.h"
  DEPS
    ${CMAKE_DL_LIBS}
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::core_headers
    iree::base::internal
    iree::base::internal::arena
    iree::base::threading
    iree::hal
    iree::hal::drivers::task::driver
    iree::hal::drivers::task::executable::loaders::registration
    iree::hal::memory::asan
    iree::hal::memory::passthrough_pool
    iree::hal::memory::slab_provider
    iree::hal::memory::tlsf_pool
    iree::hal::utils::profile_file
    iree::hal::utils::resource_set
    iree::io::file_handle
    iree::task::api
    libhrx_defs
    ${_hrx_static_vmm_test_instrumented_platform_deps}
  DEFINES
    "HRX_STATIC"
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/.>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/.>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../include>"
  PUBLIC
)

set(_hrx_platform_deps "")
if(IREE_HAL_DRIVER_AMDGPU)
  list(APPEND _hrx_platform_deps iree::hal::drivers::amdgpu)
  list(APPEND _hrx_platform_deps iree::hal::drivers::amdgpu::registration)
  list(APPEND _hrx_platform_deps iree::hal::drivers::amdgpu::target::selection)
endif()
iree_cc_library(
  NAME
    hrx
  COPTS
    "-DHRX_BUILDING_SHARED"
  HDRS
    "${LIBHRX_SOURCE_DIR}/src/iree_hal_compat.h"
    "buffer_table.h"
    "hrx_internal.h"
    "mem_pool.h"
    "transfer.h"
  SRCS
    "allocator.c"
    "buffer.c"
    "buffer_table.c"
    "buffer_view.c"
    "device.c"
    "event.c"
    "executable.c"
    "fence.c"
    "graph.c"
    "graph_analysis.c"
    "graph_exec.c"
    "host_allocator.c"
    "mem_pool.c"
    "pool.c"
    "queue_ops.c"
    "runtime.c"
    "semaphore.c"
    "status.c"
    "stream.c"
    "transfer.c"
    "vmm_slab_provider.c"
    "vmm_slab_provider.h"
  DEPS
    ${CMAKE_DL_LIBS}
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::core_headers
    iree::base::internal
    iree::base::internal::arena
    iree::base::threading
    iree::hal
    iree::hal::drivers::task::driver
    iree::hal::drivers::task::executable::loaders::registration
    iree::hal::memory::asan
    iree::hal::memory::passthrough_pool
    iree::hal::memory::slab_provider
    iree::hal::memory::tlsf_pool
    iree::hal::utils::profile_file
    iree::hal::utils::resource_set
    iree::io::file_handle
    iree::task::api
    libhrx_defs
    ${_hrx_platform_deps}
  SHARED
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/.>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/.>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../include>"
  PUBLIC
)

iree_cc_test(
  NAME
    graph_exec_test
  SRCS
    "graph_exec_test.cc"
  DEPS
    ::hrx_static
    iree::base
    iree::hal
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

iree_cc_test(
  NAME
    graph_buffer_test
  SRCS
    "graph_buffer_test.cc"
  DEPS
    ::hrx_static
    iree::base
    iree::hal
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

iree_cc_test(
  NAME
    pool_test
  SRCS
    "pool_test.cc"
  DEPS
    ::hrx_static
    iree::base
    iree::hal
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

if(IREE_HAL_DRIVER_AMDGPU)
  target_link_libraries(libhrx_src_libhrx_hrx PRIVATE
    iree::hal::drivers::amdgpu::registration
  )
  target_compile_definitions(libhrx_src_libhrx_hrx.objects PRIVATE
    HRX_HAS_IREE_AMDGPU_DRIVER
  )
endif()
# IREE's allocator selector is exposed as a compile definition on the base
# library target. libhrx is compiled through IREE's object-library wrapper, so
# forward that definition to the object targets that actually compile sources
# using iree_allocator_system().
foreach(_hrx_object_target
    libhrx_src_libhrx_hrx_static.objects
    libhrx_src_libhrx_hrx.objects)
  target_compile_definitions(${_hrx_object_target} PRIVATE
    $<TARGET_PROPERTY:iree_base_base,INTERFACE_COMPILE_DEFINITIONS>
  )
  foreach(_hrx_generated_header_target
      op_table_gen
      encoding_table_gen)
    if(TARGET ${_hrx_generated_header_target})
      add_dependencies(${_hrx_object_target} ${_hrx_generated_header_target})
    endif()
  endforeach()
endforeach()
set_target_properties(libhrx_src_libhrx_hrx PROPERTIES
  OUTPUT_NAME hrx
  VERSION ${PROJECT_VERSION}
  SOVERSION 0
  EXPORT_NAME hrx
  POSITION_INDEPENDENT_CODE ON
)
if(APPLE)
  set_target_properties(libhrx_src_libhrx_hrx PROPERTIES
    BUILD_RPATH "@loader_path"
    INSTALL_RPATH "@loader_path"
    C_VISIBILITY_PRESET hidden
  )
  set(_hrx_export_file "${LIBHRX_SOURCE_DIR}/cmake/hrx_exports_macos.txt")
  target_link_options(libhrx_src_libhrx_hrx PRIVATE
    "-Wl,-exported_symbols_list,${_hrx_export_file}"
  )
  set_property(TARGET libhrx_src_libhrx_hrx APPEND PROPERTY
    LINK_DEPENDS "${_hrx_export_file}"
  )
elseif(UNIX)
  set_target_properties(libhrx_src_libhrx_hrx PROPERTIES
    BUILD_RPATH "$ORIGIN"
    INSTALL_RPATH "$ORIGIN"
    C_VISIBILITY_PRESET hidden
  )
  set(_hrx_export_file "${LIBHRX_SOURCE_DIR}/cmake/hrx_exports.lds")
  target_link_options(libhrx_src_libhrx_hrx PRIVATE
    "-Wl,--version-script=${_hrx_export_file}"
  )
  set_property(TARGET libhrx_src_libhrx_hrx APPEND PROPERTY
    LINK_DEPENDS "${_hrx_export_file}"
  )
endif()
set_property(TARGET libhrx_src_libhrx_hrx PROPERTY
  INTERFACE_LINK_LIBRARIES ""
)
if(NOT TARGET hrx)
  add_custom_target(hrx DEPENDS libhrx_src_libhrx_hrx)
endif()
if(NOT TARGET hrx::hrx)
  iree_add_alias_library(hrx::hrx libhrx_src_libhrx_hrx)
endif()
