################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/hal/drivers/amdgpu/device/BUILD.bazel                       #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

add_custom_command(OUTPUT bitcode_srcs.stamp
    COMMAND ${CMAKE_COMMAND} -E touch bitcode_srcs.stamp
  DEPENDS
    "atomic.c"
    "blit.c"
    "dispatch.c"
    "grid_sync.c"
    "timestamp.c"
    "tsan.c"
)

add_custom_target(bitcode_srcs
    DEPENDS bitcode_srcs.stamp
)
iree_register_generated_compile_input(bitcode_srcs
  OUTPUTS
    "${CMAKE_CURRENT_BINARY_DIR}/bitcode_srcs.stamp"
)

add_custom_command(OUTPUT bitcode_hdrs.stamp
    COMMAND ${CMAKE_COMMAND} -E touch bitcode_hdrs.stamp
  DEPENDS
    "atomic.h"
    "blit.h"
    "dispatch.h"
    "grid_sync.h"
    "kernel_tables.h"
    "kernels.h"
    "support/asan.h"
    "support/common.h"
    "support/feedback.h"
    "support/kernel.h"
    "support/queue.h"
    "support/signal.h"
    "support/tsan.h"
    "timestamp.h"
    "tsan.h"
)

add_custom_target(bitcode_hdrs
    DEPENDS bitcode_hdrs.stamp
)
iree_register_generated_compile_input(bitcode_hdrs
  OUTPUTS
    "${CMAKE_CURRENT_BINARY_DIR}/bitcode_hdrs.stamp"
)

if(IREE_HAL_DRIVER_AMDGPU)
iree_amdgpu_library_variants(
  NAME
    asanrtl
  TARGET
    amdgcn-amd-amdhsa
  TARGETS
    ${IREE_HAL_AMDGPU_TARGETS}
  INTERNAL_HDRS
    "${IREE_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/abi/amdgpu_abi_bitcode_hdrs.stamp"
    "bitcode_hdrs.stamp"
  SRCS
    "asanrtl.c"
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    headers
  HDRS
    "atomic.h"
    "blit.h"
    "dispatch.h"
    "grid_sync.h"
    "kernel_tables.h"
    "kernels.h"
    "support/asan.h"
    "support/common.h"
    "support/feedback.h"
    "support/kernel.h"
    "support/queue.h"
    "support/signal.h"
    "support/tsan.h"
    "timestamp.h"
    "tsan.h"
  DEPS
    iree::base
    iree::base::internal
    iree::base::threading::thread
    iree::hal::drivers::amdgpu::abi
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    atomic
  SRCS
    "atomic.c"
  DEPS
    ::dispatch
    ::headers
    iree::hal
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    atomic_pm4
  HDRS
    "atomic_pm4.h"
  SRCS
    "atomic_pm4.c"
  DEPS
    ::headers
    ::kernel_pm4
    iree::hal
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    blit
  SRCS
    "blit.c"
  DEPS
    ::headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    blit_pm4
  HDRS
    "blit_pm4.h"
  SRCS
    "blit_pm4.c"
  DEPS
    ::blit
    ::headers
    ::kernel_pm4
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    dispatch
  SRCS
    "dispatch.c"
  DEPS
    ::headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    grid_sync
  SRCS
    "grid_sync.c"
  DEPS
    ::dispatch
    ::headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    kernel_pm4
  HDRS
    "kernel_pm4.h"
  SRCS
    "kernel_pm4.c"
  DEPS
    ::headers
    iree::hal::drivers::amdgpu::util::libhsa_headers
    iree::hal::drivers::amdgpu::util::loaded_code_object
    iree::hal::drivers::amdgpu::util::pm4_dispatch
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    timestamp
  SRCS
    "timestamp.c"
  DEPS
    ::dispatch
    ::headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    tsan
  SRCS
    "tsan.c"
  DEPS
    ::dispatch
    ::headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    atomic_live_test
  SRCS
    "atomic_live_test.cc"
  DEPS
    ::atomic
    ::headers
    iree::base
    iree::base::internal
    iree::hal::drivers::amdgpu::util::device_library
    iree::hal::drivers::amdgpu::util::libhsa
    iree::hal::drivers::amdgpu::util::libhsa_headers
    iree::hal::drivers::amdgpu::util::packet
    iree::hal::drivers::amdgpu::util::queue_primitives
    iree::hal::drivers::amdgpu::util::topology
    iree::hal::drivers::amdgpu::util::vmem
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    atomic_test
  SRCS
    "atomic_test.cc"
  DEPS
    ::atomic
    ::headers
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    blit_test
  SRCS
    "blit_test.cc"
  DEPS
    ::blit
    ::headers
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    dispatch_test
  SRCS
    "dispatch_test.cc"
  DEPS
    ::dispatch
    ::headers
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    grid_sync_test
  SRCS
    "grid_sync_test.cc"
  DEPS
    ::grid_sync
    ::headers
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    timestamp_test
  SRCS
    "timestamp_test.cc"
  DEPS
    ::headers
    ::timestamp
    iree::hal::drivers::amdgpu::abi
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    tsan_test
  SRCS
    "tsan_test.cc"
  DEPS
    ::headers
    ::tsan
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
