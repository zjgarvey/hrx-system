################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/hal/drivers/amdgpu/util/BUILD.bazel                         #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    libhsa_headers
  HDRS
    "libhsa.h"
  TEXTUAL_HDRS
    "libhsa_tables.h"
  DEPS
    iree::base
    iree::third_party::hsa_runtime_headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    libhsa
  SRCS
    "libhsa.c"
  DEPS
    ::libhsa_headers
    iree::base
    iree::base::internal
    iree::base::internal::dynamic_library
    iree::base::internal::path
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    libhsa_test_instrumented
  SRCS
    "libhsa.c"
  DEPS
    ::libhsa_headers
    iree::base
    iree::base::internal
    iree::base::internal::dynamic_library
    iree::base::internal::path
    iree::base::threading
  DEFINES
    "IREE_HAL_AMDGPU_LIBHSA_TEST_INSTRUMENTATION"
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    libaqlprofile
  HDRS
    "libaqlprofile.h"
  SRCS
    "libaqlprofile.c"
  DEPS
    ::libhsa_headers
    iree::base
    iree::base::internal
    iree::base::internal::dynamic_library
    iree::base::internal::path
    iree::hal::drivers::amdgpu::abi
    iree::third_party::aqlprofile_sdk_headers
    iree::third_party::hsa_runtime_headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    libhsa_test
  SRCS
    "libhsa_test.cc"
  DEPS
    ::libhsa
    ::libhsa_headers
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    libhsa_test_instrumented_test
  SRCS
    "libhsa_test.cc"
  DEFINES
    "IREE_HAL_AMDGPU_LIBHSA_TEST_INSTRUMENTATION"
  DEPS
    ::libhsa_headers
    ::libhsa_test_instrumented
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    libaqlprofile_test
  SRCS
    "libaqlprofile_test.cc"
  DEPS
    ::libaqlprofile
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    topology
  HDRS
    "topology.h"
  SRCS
    "topology.c"
  DEPS
    ::libhsa_headers
    iree::base
    iree::hal
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    agent_target
  HDRS
    "agent_target.h"
  SRCS
    "agent_target.c"
  DEPS
    ::libhsa_headers
    iree::base
    iree::hal::drivers::amdgpu::target::identity
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    agent_target_test
  SRCS
    "agent_target_test.cc"
  DEPS
    ::agent_target
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    topology_test
  SRCS
    "topology_test.cc"
  DEPS
    ::libhsa
    ::libhsa_headers
    ::topology
    iree::base
    iree::hal
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    hsaco_metadata
  HDRS
    "hsaco_metadata.h"
  SRCS
    "hsaco_metadata.c"
  DEPS
    iree::base
    iree::hal
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hsaco_metadata_test
  SRCS
    "hsaco_metadata_test.cc"
  DEPS
    ::hsaco_metadata
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    global_table
  HDRS
    "global_table.h"
  SRCS
    "global_table.c"
  DEPS
    iree::base
    iree::base::internal
    iree::base::threading
    iree::hal
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    global_table_test
  SRCS
    "global_table_test.cc"
  DEPS
    ::global_table
    iree::base
    iree::hal
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_binary_benchmark(
  NAME
    global_table_benchmark
  SRCS
    "global_table_benchmark.cc"
  DEPS
    ::global_table
    iree::base
    iree::hal
    iree::testing::benchmark
    iree::testing::benchmark_main
    iree::third_party::google_benchmark
  TESTONLY
  LABELS
    "benchmark"
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_fuzz(
  NAME
    hsaco_metadata_fuzz
  SRCS
    "hsaco_metadata_fuzz.cc"
  DEPS
    ::hsaco_metadata
    iree::base
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    affinity
  HDRS
    "affinity.h"
  DEPS
    iree::base::internal
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    affinity_test
  SRCS
    "affinity_test.cc"
  DEPS
    ::affinity
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    packet
  HDRS
    "aql_emitter.h"
    "pm4_barrier.h"
    "pm4_capabilities.h"
    "pm4_emitter.h"
  DEPS
    iree::base
    iree::hal::drivers::amdgpu::abi
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    pm4_atomic
  HDRS
    "pm4_atomic.h"
  DEPS
    ::packet
    iree::base
    iree::hal
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    info
  HDRS
    "info.h"
  SRCS
    "info.c"
  DEPS
    ::libhsa_headers
    iree::base
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    device_clock
  HDRS
    "device_clock.h"
  SRCS
    "device_clock.c"
  DEPS
    ::kfd
    iree::base
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    kfd
  HDRS
    "kfd.h"
  SRCS
    "kfd.c"
  DEPS
    iree::base
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    pm4_program
  HDRS
    "pm4_program.h"
  SRCS
    "pm4_program.c"
  DEPS
    ::libhsa_headers
    ::packet
    iree::base
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    pm4_dispatch
  HDRS
    "pm4_dispatch.h"
  SRCS
    "pm4_dispatch.c"
  DEPS
    ::packet
    iree::base
    iree::hal::drivers::amdgpu::abi
    iree::hal::drivers::amdgpu::target::identity
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    queue_primitives
  HDRS
    "aql_ring.h"
    "epoch_signal_table.h"
    "kernarg_ring.h"
    "notification_ring.h"
    "queue_upload_ring.h"
    "signal_pool.h"
  SRCS
    "aql_ring.c"
    "kernarg_ring.c"
    "notification_ring.c"
    "queue_upload_ring.c"
    "signal_pool.c"
  DEPS
    ::libhsa_headers
    iree::async
    iree::base
    iree::base::internal
    iree::base::internal::arena
    iree::base::threading
    iree::hal
    iree::hal::drivers::amdgpu::abi
    iree::hal::utils::resource_set
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    vmem
  HDRS
    "vmem.h"
  SRCS
    "vmem.c"
  DEPS
    ::libhsa_headers
    ::topology
    iree::base
    iree::hal
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    feedback_channel
  HDRS
    "feedback_channel.h"
  SRCS
    "feedback_channel.c"
  DEPS
    ::libhsa_headers
    ::topology
    ::vmem
    iree::base
    iree::base::internal
    iree::hal::drivers::amdgpu::abi
    iree::hal::drivers::amdgpu::device::headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    shadow_map
  HDRS
    "shadow_map.h"
  SRCS
    "shadow_map.c"
  DEPS
    ::libhsa_headers
    ::vmem
    iree::base
    iree::base::internal
    iree::base::threading
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    block_pool
  HDRS
    "block_pool.h"
  SRCS
    "block_pool.c"
  DEPS
    ::libhsa_headers
    iree::base
    iree::base::threading
    iree::hal::memory::tracing
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    device_library
  HDRS
    "device_library.h"
  SRCS
    "device_library.c"
  DEPS
    ::agent_target
    ::device_library_target
    ::libhsa_headers
    ::loaded_code_object
    ::topology
    iree::base
    iree::base::internal
    iree::hal::drivers::amdgpu::device::binaries::toc
    iree::hal::drivers::amdgpu::device::headers
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    loaded_code_object
  HDRS
    "loaded_code_object.h"
  SRCS
    "loaded_code_object.c"
  DEPS
    ::libhsa_headers
    iree::base
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    device_library_target
  HDRS
    "device_library_target.h"
  TEXTUAL_HDRS
    "device_library_target_map.inl"
  SRCS
    "device_library_target.c"
  DEPS
    iree::base
    iree::hal::drivers::amdgpu::target::identity
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    util
  DEPS
    ::affinity
    ::agent_target
    ::block_pool
    ::device_library
    ::device_library_target
    ::feedback_channel
    ::global_table
    ::info
    ::kfd
    ::loaded_code_object
    ::packet
    ::pm4_dispatch
    ::pm4_program
    ::queue_primitives
    ::vmem
    iree::hal::drivers::amdgpu::target::identity
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    benchmark_flags
  HDRS
    "benchmark_flags.h"
  SRCS
    "benchmark_flags.cc"
  DEPS
    iree::async
    iree::base
    iree::base::tooling::flags
    iree::third_party::google_benchmark
  TESTONLY
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    benchmark_profile
  HDRS
    "benchmark_profile.h"
  SRCS
    "benchmark_profile.cc"
  DEPS
    iree::base
    iree::hal
  TESTONLY
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_binary_benchmark(
  NAME
    blit_benchmark
  SRCS
    "blit_benchmark.cc"
  DEPS
    ::benchmark_flags
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::threading
    iree::base::tooling::flags
    iree::hal
    iree::hal::drivers::amdgpu::registration
    iree::third_party::google_benchmark
  TESTONLY
  LABELS
    "benchmark"
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    block_pool_test
  SRCS
    "block_pool_test.cc"
  DEPS
    ::block_pool
    ::topology
    ::vmem
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    device_library_test
  SRCS
    "device_library_test.cc"
  DEPS
    ::device_library
    ::topology
    iree::base
    iree::hal::drivers::amdgpu::device::headers
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    device_library_target_test
  SRCS
    "device_library_target_test.cc"
  DEPS
    ::device_library_target
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    epoch_signal_table_test
  SRCS
    "epoch_signal_table_test.cc"
  DEPS
    ::queue_primitives
    iree::async
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    kernarg_ring_test
  SRCS
    "kernarg_ring_test.cc"
  DEPS
    ::queue_primitives
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    aql_ring_test
  SRCS
    "aql_ring_test.cc"
  DEPS
    ::libhsa
    ::libhsa_headers
    ::packet
    ::queue_primitives
    iree::base
    iree::hal::drivers::amdgpu::abi
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    queue_upload_ring_test
  SRCS
    "queue_upload_ring_test.cc"
  DEPS
    ::queue_primitives
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    device_clock_test
  SRCS
    "device_clock_test.cc"
  DEPS
    ::device_clock
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    kfd_test
  SRCS
    "kfd_test.cc"
  DEPS
    ::kfd
    ::libhsa
    ::libhsa_headers
    ::topology
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    notification_ring_test
  SRCS
    "notification_ring_test.cc"
  DEPS
    ::libhsa
    ::libhsa_headers
    ::queue_primitives
    iree::async
    iree::async::platform
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    aql_emitter_test
  SRCS
    "aql_emitter_test.cc"
  DEPS
    ::packet
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    pm4_emitter_test
  SRCS
    "pm4_emitter_test.cc"
  DEPS
    ::packet
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    pm4_barrier_test
  SRCS
    "pm4_barrier_test.cc"
  DEPS
    ::packet
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    pm4_atomic_test
  SRCS
    "pm4_atomic_test.cc"
  DEPS
    ::pm4_atomic
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    pm4_dispatch_test
  SRCS
    "pm4_dispatch_test.cc"
  DEPS
    ::packet
    ::pm4_dispatch
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_amdgpu_binary_variants_embed_data(
  NAME
    pm4_dispatch_test_kernels
  TARGET
    amdgcn-amd-amdhsa
  TARGETS
    ${IREE_HAL_AMDGPU_TARGETS}
  BINARY_NAME_PREFIX
    pm4_dispatch_test_kernels
  C_FILE_OUTPUT
    "pm4_dispatch_test_kernels.c.embed.c"
  H_FILE_OUTPUT
    "pm4_dispatch_test_kernels.h"
  IDENTIFIER
    "iree_hal_amdgpu_pm4_dispatch_test_kernels"
  SRCS
    "pm4_dispatch_test_kernels.c"
  TESTONLY
  FLATTEN
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    pm4_dispatch_live_test
  SRCS
    "pm4_dispatch_live_test.cc"
  DEPS
    ::libhsa
    ::libhsa_headers
    ::packet
    ::pm4_atomic
    ::pm4_dispatch
    ::pm4_dispatch_test_kernels
    ::pm4_program
    ::queue_primitives
    ::topology
    ::vmem
    iree::base
    iree::base::threading
    iree::hal::drivers::amdgpu::abi
    iree::hal::drivers::amdgpu::target::identity
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    pm4_program_test
  SRCS
    "pm4_program_test.cc"
  DEPS
    ::packet
    ::pm4_program
    ::topology
    ::vmem
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_amdgpu_hal_cts_testdata(
  NAME
    testdata_amdgpu_queue_benchmark
  TARGET
    amdgcn-amd-amdhsa
  TARGETS
    ${IREE_HAL_AMDGPU_TARGETS}
  TARGET_NAME
    amdgpu_queue_benchmark
  IDENTIFIER
    "iree_queue_benchmark_testdata_amdgpu"
  BACKEND_NAME
    "amdgpu"
  TARGET_FAMILY
    "amdgpu"
  INTERNAL_HDRS
    "${IREE_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/abi/amdgpu_abi_bitcode_hdrs.stamp"
    "${IREE_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/device/bitcode_hdrs.stamp"
  SRCS
    "queue_benchmark_testdata.c"
  TESTONLY
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_binary_benchmark(
  NAME
    queue_benchmark
  SRCS
    "queue_benchmark.cc"
  DEPS
    ::benchmark_flags
    ::testdata_amdgpu_queue_benchmark
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::threading
    iree::base::tooling::flags
    iree::hal
    iree::hal::cts::util::registry
    iree::hal::drivers::amdgpu
    iree::hal::drivers::amdgpu::cts::testdata_amdgpu
    iree::hal::drivers::amdgpu::device::dispatch
    iree::hal::drivers::amdgpu::device::headers
    iree::hal::drivers::amdgpu::headers
    iree::hal::drivers::amdgpu::registration
    iree::hal::drivers::amdgpu::target::selection
    iree::hal::memory::tlsf_pool
    iree::io::file_handle
    iree::third_party::google_benchmark
  TESTONLY
  LABELS
    "benchmark"
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    signal_pool_test
  SRCS
    "signal_pool_test.cc"
  DEPS
    ::libhsa
    ::queue_primitives
    ::topology
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_binary_benchmark(
  NAME
    signal_pool_benchmark
  SRCS
    "signal_pool_benchmark.cc"
  DEPS
    ::libhsa
    ::queue_primitives
    ::topology
    iree::base
    iree::third_party::google_benchmark
  TESTONLY
  LABELS
    "benchmark"
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    vmem_test
  SRCS
    "vmem_test.cc"
  DEPS
    ::libhsa
    ::topology
    ::vmem
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    feedback_channel_test
  SRCS
    "feedback_channel_test.cc"
  DEPS
    ::feedback_channel
    ::libhsa
    ::topology
    iree::base
    iree::hal::drivers::amdgpu::device::headers
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_amdgpu_binary_variants_embed_data(
  NAME
    feedback_channel_test_kernels
  TARGET
    amdgcn-amd-amdhsa
  TARGETS
    ${IREE_HAL_AMDGPU_TARGETS}
  BINARY_NAME_PREFIX
    feedback_channel_test_kernels
  C_FILE_OUTPUT
    "feedback_channel_test_kernels.c.embed.c"
  H_FILE_OUTPUT
    "feedback_channel_test_kernels.h"
  IDENTIFIER
    "iree_hal_amdgpu_feedback_channel_test_kernels"
  INTERNAL_HDRS
    "${IREE_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/abi/amdgpu_abi_bitcode_hdrs.stamp"
    "${IREE_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/device/bitcode_hdrs.stamp"
  SRCS
    "feedback_channel_test_kernels.c"
  TESTONLY
  FLATTEN
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    feedback_channel_live_test
  SRCS
    "feedback_channel_live_test.cc"
  DEPS
    ::feedback_channel
    ::feedback_channel_test_kernels
    ::libhsa
    ::libhsa_headers
    ::packet
    ::queue_primitives
    ::topology
    iree::base
    iree::hal::drivers::amdgpu::abi
    iree::hal::drivers::amdgpu::target::identity
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    shadow_map_test
  SRCS
    "shadow_map_test.cc"
  DEPS
    ::shadow_map
    iree::base
    iree::testing::gtest
    iree::testing::gtest_main
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

# Enable statically linking against ROCR-Runtime.
# The resulting application binary will initialize the runtime at startup and
# may fail to work on any machine without a modern ROCM driver. Avoid using this
# unless it is known that the deployment is always on an up-to-date AMDGPU
# device.
option(IREE_HAL_AMDGPU_LIBHSA_STATIC
       "Statically link against the installed hsa-runtime64 package."
       OFF)

if(IREE_HAL_AMDGPU_LIBHSA_STATIC)
  if(NOT TARGET iree::third_party::hsa_runtime)
    message(FATAL_ERROR
      "IREE_HAL_AMDGPU_LIBHSA_STATIC=ON requires a ROCm/TheRock package "
      "configuration that provides hsa-runtime64::hsa-runtime64. Header-only "
      "ROCm discovery is enough for dynamic loading but cannot satisfy static "
      "libhsa linking.")
  endif()
  target_compile_options(iree_hal_drivers_amdgpu_util_libhsa
    PUBLIC
      "-DIREE_HAL_AMDGPU_LIBHSA_STATIC=1"
  )
  target_link_libraries(iree_hal_drivers_amdgpu_util_libhsa
    PRIVATE
      iree::third_party::hsa_runtime
  )
endif()
